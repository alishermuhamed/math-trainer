import * as React from 'react';
import { Header, Body } from './components';

const App: React.FC = (): JSX.Element => (
  <React.Fragment>
    <Header />
    <Body />
  </React.Fragment>
);

export default App;
